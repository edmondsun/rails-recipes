<h1><%= t("event_list") %></h1>

<ul>
  <% @events.each do |event| %>
    <li>
      <!-- <%= link_to event.name, event_path(event) %> -->
      <!-- <%= link_to event.name, event_path(event) %> <%= event.created_at.strftime("%Y/%m/%d %I:%M %p") %> -->
      <!-- <%= link_to event.name, event_path(event) %> <%= event.created_at.to_s(:ymd) %> -->
      <%= link_to event.name, event_path(event) %> <%= event.created_at %>
    </li>
  <% end %>
</ul>
